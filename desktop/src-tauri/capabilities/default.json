{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal M0 permissions for the main window: core defaults (window/webview/app/path/resources) already include event emit/listen, needed for the backend-failed / backend-ready handshake events. Custom app commands (get_backend_info, retry_backend) are allowed by default for all windows and need no explicit entry.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "dialog:allow-save",
    "fs:allow-write-file"
  ]
}
